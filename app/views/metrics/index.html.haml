%h1 Metrics#index
%p Blah blah... app/views/msrun/index.html.erb
- @metrics.each do |metric|
  - m = Msrun.get(metric.msrun_id)
  = "#{m.raw_id} was run on #{m.rawtime.readable_inspect} "
  %p -------------
